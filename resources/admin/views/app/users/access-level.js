import React, { Suspense, useState } from "react";

const AccessLevel = ({ match }) => {
  return (
    <Suspense fallback={<div className="loading" />}>

    </Suspense>
  );
}
export default AccessLevel;
